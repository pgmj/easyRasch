# Create a DIF line graph, showing groups' RM item locations

Create a DIF line graph, showing groups' RM item locations

## Usage

``` r
RIdifFigureRM(dfin, dif.var)
```

## Arguments

- dfin:

  Dataframe with item data only

- dif.var:

  DIF variable
