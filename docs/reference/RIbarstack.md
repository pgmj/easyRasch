# Create a stacked bar graph to show response distribution

Create a stacked bar graph to show response distribution

## Usage

``` r
RIbarstack(dfin, omit.na = T)
```

## Arguments

- dfin:

  Dataframe with item data only

- omit.na:

  Remove respondents with missing data (or not)
