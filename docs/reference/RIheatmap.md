# Create a Guttman-like tileplot/heatmap

Sorts items (y axis) and persons (x axis) according to respective total
score and displays them with a color gradient based on responses

## Usage

``` r
RIheatmap(dfin)
```

## Arguments

- dfin:

  Dataframe with item data only
