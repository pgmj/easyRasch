# Parametric bootstrapping function that outputs simulated datasets

For generic use. Outputs datasets to a list object.

## Usage

``` r
RIpboot(data, iterations, cpu = 4)
```

## Arguments

- data:

  A dataframe with response data

- iterations:

  How many datasets to generate

- cpu:

  Number of CPU cores to use
