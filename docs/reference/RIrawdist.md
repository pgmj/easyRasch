# Floor/ceiling effects based on raw data (ordinal scores). Needs at least one data point in each response category to produce correct footnote text.

Floor/ceiling effects based on raw data (ordinal scores). Needs at least
one data point in each response category to produce correct footnote
text.

## Usage

``` r
RIrawdist(dfin)
```

## Arguments

- dfin:

  Dataframe with item data only

## Value

A barplot with descriptives in footnote
