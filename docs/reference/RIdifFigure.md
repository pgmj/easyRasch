# Create a DIF line graph, showing groups' PCM item locations

Create a DIF line graph, showing groups' PCM item locations

## Usage

``` r
RIdifFigure(dfin, dif.var)
```

## Arguments

- dfin:

  Dataframe with item data only

- dif.var:

  DIF variable
